# Context Orb Design

## Scene and theme

开发者在普通室内光线下，长时间盯着代码，只偶尔扫向窗口边缘确认状态。深靛球壳与体积光带形成识别点，面板使用冷灰中性色和细分隔线，提供相同层级的深色模式。方向与复审依据见 docs/ORB-REDESIGN-V1.md。

## Color strategy

灰雾表示未评估，青蓝光带表示所列依据一致，局部琥珀干扰表示待核验线索，珊瑚分叉表示已发现检查偏差。容量用独立的冷色进度环，未知时虚线；状态文字、形状和动作图标同时保留。

## Type

沿用本地 DM Sans，中文使用系统 PingFang SC / Microsoft YaHei。面板正文 12px、次要标签 10px、状态标题 22px。计量值使用等宽数字，未知值显示文字而不是 0。

## Surface and layout

原生折叠 92×92px，悬停预览请求 320×240px，详情请求 382×690px。球核 70px、容量环 84px、含焦点总绘制范围 90px。可用空间不足时缩小窗口并在详情内部滚动；预览只在能够容纳时显示。主操作位于指标之后、证据详情之前。压缩次数默认隐藏，可在设置中开启。

## Interaction

磁吸采用拖中自由移动、松手必停靠和拖后点击抑制。窗口吸附三档：仅限 Codex 窗口、关闭、所有窗口。在允许的窗口内松手，选择其最近边框；其他位置选择屏幕最近边缘。不提供 CLI、手动窗口绑定或屏幕吸附开关；平台无法可靠识别 Codex 时明确显示不可用。

悬停或聚焦 350ms 后显示被动预览；单击、Enter、Space 执行已展示的主动作，右键、Shift F10 和上下文菜单键打开详情。Escape 收起并恢复球体焦点，不立即重复打开预览。按下锁定任务与依据，释放前复核，改变时撤销动作。

面板朝屏幕内展开，保持球的全局位置。尺寸请求序列化并跳过尚未发送的旧请求；原生手势期间暂停尺寸变化。球内仅使用 transform/opacity 缓慢流动，可独立关闭，拖动、隐藏和离屏时暂停。减少动态效果时移除装饰动画。
