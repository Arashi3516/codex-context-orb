# Context Orb Design

## Scene and theme

开发者在普通室内光线下，长时间盯着代码，只偶尔扫向窗口边缘确认状态。默认用低对比的暖灰背景与陶瓷表面，提供相同层级的深色模式。

## Color strategy

克制的中性色与明确的语义色。以 OKLCH 定义新状态色：灰色未评估，柔和绿色表示所列依据一致，琥珀色表示待核验线索，暖橙色表示已发现检查偏差。容量用独立的冷色进度环；状态文字和图形同时保留。

## Type

沿用本地 DM Sans，中文使用系统 PingFang SC / Microsoft YaHei。面板正文 11px、标签 9–10px、状态标题 22px。计量值使用等宽数字，未知值显示文字而不是 0。

## Surface and layout

原生折叠 92px，展开窗口请求尺寸 382×690px，可用空间不足时缩小窗口并在面板内部滚动。状态总览采用分区和细分隔线，详情中才使用独立收据条目。球体颜色承载风险状态，外环承载容量，压缩次数默认隐藏，可在设置中开启。

## Interaction

磁吸采用拖中自由移动、松手必停靠和拖后点击抑制。窗口吸附三档：仅限 Codex 窗口、关闭、所有窗口。在允许的窗口内松手，选择其最近边框；其他位置选择屏幕最近边缘。不提供 CLI、手动窗口绑定或屏幕吸附开关；平台无法可靠识别 Codex 时明确显示不可用。

面板朝屏幕内展开，改变展开方向时保持已停靠球的位置。颜色过渡约 200ms，拖拽直接响应，不使用装饰性弹跳。减少动态效果时关闭过渡和非必要位移效果。
